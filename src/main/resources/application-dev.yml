spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: ${DEV_DB_USERNAME}
    password: ${DEV_DB_PASSWORD}
    url: jdbc:mysql://localhost:3306/voy_tuber?serverTimezone=UTC
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect